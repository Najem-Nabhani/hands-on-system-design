## System Evolution 

Resource: https://github.com/tenxxor/distributed-systems-from-first-principles/tree/main

## Product stages
1. A simple terminal-based like/dislike counter for a single user. The user can press `l` to like, `d` to dislike, or `c` to clear their vote, and `q` to quit. And in return, the counter will show the current state of the counter.

## Code stages
1. Basic commad-based Kotlin application that uses: [Command pattern]()
